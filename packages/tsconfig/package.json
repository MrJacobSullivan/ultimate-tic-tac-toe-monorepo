{
  "name": "tsconfig",
  "version": "0.0.0",
  "private": true,
  "files": [
    "nextjs.json",
    "node.json"
  ],
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  }
}
