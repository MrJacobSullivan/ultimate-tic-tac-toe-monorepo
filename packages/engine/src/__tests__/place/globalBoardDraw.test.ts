import { place } from '../../actions';
import { initialState } from '../../lib/constants';
import { CoordinatePair } from '../../types';

describe('globalBoardDraw', () => {
  let state = initialState;

  test('handles global board draw', () => {
    const expectedState = {};

    const moves: CoordinatePair[] = [];

    expect(1).toBe(1);
  });
});
