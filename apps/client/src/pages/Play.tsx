import * as React from 'react';
import GameBoard from '../components/Game/GameBoard';

const Play = () => {
  return (
    <div>
      <p>Play</p>

      <GameBoard />
    </div>
  );
};

export default Play;
